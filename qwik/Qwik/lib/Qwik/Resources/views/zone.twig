{% for moduleInfo in this.getModules() %}
    {{ app.qwik_module.render(moduleInfo) }}
{#    {% set module = app.qwik_module.getModule(this, moduleKey, moduleConfig) %}
    {{ app.twig.render(module.getTemplatePath(), module.getTemplateVars()|merge({module:module})) }}
#}
{% endfor %}
